function main()
{
   // Widget instantiation metadata...
   var taskParticipants = {
      id : "TaskParticipants",
      name : "Alvex.TaskParticipants"
   };
   model.widgets = [taskParticipants];
}

main();